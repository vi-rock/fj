[project]
name = "observability"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "structlog>=26.1.0",
    "config",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
